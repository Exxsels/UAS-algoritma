DEKLARASI
    nama[100], merek[100] : string
    harga[100] : integer
    jumlah, pilihan : integer

ALGORITMA
    jumlah ← 0

    SELAMA true
        TAMPILKAN menu
        INPUT pilihan

        JIKA pilihan = 1
            INPUT nama[jumlah], merek[jumlah], harga[jumlah]
            jumlah ← jumlah + 1

        JIKA pilihan = 2
            JIKA jumlah = 0
                TAMPILKAN "Data kosong"
            ELSE
                UNTUK i ← 0 SAMPAI jumlah - 1
                    TAMPILKAN data mobil
                SELESAI
            ENDIF

        JIKA pilihan = 3
            TAMPILKAN data mobil
            INPUT index
            UPDATE data mobil pada index

        JIKA pilihan = 4
            TAMPILKAN data mobil
            INPUT index
            HAPUS data mobil pada index
            jumlah ← jumlah - 1

        JIKA pilihan = 5
            KELUAR PROGRAM
        ENDIF
    ENDWHILE

SELESAI
